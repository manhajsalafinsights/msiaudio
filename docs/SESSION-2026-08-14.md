# Sesi Simpan — 14 Agustus 2026

Semua pekerjaan sudah ter-commit & ter-push ke `origin` (xroya24-pixel/msiaudio) dan `backup` (manhajsalafinsights/msiaudio — terhubung Vercel).

## 1. Yang selesai (no. commit terbaru → terlama)

- `5104c4d` Lanjutkan Belajar: item kecil → animasi CD (AudioDisc) & diperkecil (h-12).
- `b42d8c6` Home stats: format compact "rb" (7,1 rb).
- `ac908b5` Home stats: hapus akhiran rb.
- `8b049dd` Home stats: hapus item Pemateri → sisa 4 statistik.
- `7546f02` Home stats: ikon + angka saja, tanpa label.
- `3f2bee8` Home stats: tambah Pendengar (±7k, headset) & Pengguna (985, orang) — berubah tiap jam (`hourlyValue`). SEMENTARA: ganti ke data real (`countUsers`) nanti setelah web ramai.
- `0b7ca87` Home Kategori: chip kecil bisa digeser, tanpa judul & tanpa chip "Lihat semua".
- `767f6b2` Footer: Tentang/Kebijakan Privasi/Syarat & Ketentuan → popup modal (`legal-links.tsx` + `lib/config/legal-content.ts`), bukan halaman.
- `28d975d` Home Kategori: kartu ber-ikon (digantikan chips oleh 0b7ca87).
- `dde706b` (sebelumnya) Auto-detect tipe series + Rapikan Series — status deploy saat itu belum dikonfirmasi.

## 2. Domain msiaudio.manhajsalafinsights.com — STATUS: Valid ✅

- Nameserver `manhajsalafinsights.com` = **Vercel DNS** (ns1/ns2.vercel-dns.com) — bukan semutdata. Semua record DNS dikelola di Vercel (akun pemegang domain, project `msi` & `perpustakaan-msi`).
- Record final di Catatan DNS (akun pemegang domain):
  - `msiaudio` CNAME → `15dc164c133a7fb8.vercel-dns-017.com.`
  - `_vercel` TXT → `vc-domain-verify=msiaudio.manhajsalafinsights.com,993ab9b6fcf57b3de2f0` (token BARU — token lama `9e43cbad…` tidak berlaku)
  - `_vercel` TXT lama `pustaka…` tetap utuh.
- CNAME yang ditambahkan di cPanel semutdata TIDAK dipakai (nameserver sudah pindah).
- `manhajsalafinsights.com` tetap berfungsi; dua-duanya serve app yang sama.

## 3. Tindak lanjut (BLM selesai)

- [ ] Vercel → project `msi` → Environment Variables → Production: ganti `NEXT_PUBLIC_APP_URL` (dan `AUTH_URL` kalau ada) = `https://msiaudio.manhajsalafinsights.com` → **Redeploy**.
- [ ] Cek akses `https://msiaudio.manhajsalafinsights.com`.
- [ ] Deploy status lama `dde706b` (error build) — belum dikonfirmasi log Vercel-nya.
- [ ] Import channel: kuota YouTube Data API 10.000 unit/hari (reset ~14:00 WIB) — bisa diajukan naik ke 1 juta di Google Cloud Console.

## 4. Catatan teknis lainnya

- `DATABASE_URL` di Vercel wajib pakai transaction pooler 6543 + `?pgbouncer=true&connection_limit=1` — cegah EMAXCONN (max 200 koneksi) saat prerender paralel (sitemap dsb). Catatan lengkap di `.env.supabase` (tidak di-commit, sengaja).
- File `docs/LAPORAN-FITUR.md` & `player.jpg` sengaja TIDAK di-commit (bukan bagian proyek).