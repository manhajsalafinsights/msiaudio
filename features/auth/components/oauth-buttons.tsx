/**
 * Tempat tombol OAuth (Google, dst.) di masa depan (architecture.md §9.3).
 * OAuth belum dikonfigurasi pada fase ini — komponen dirender kosong.
 */
export function OAuthButtons() {
  return null;
}
